name: Tests

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

permissions:
  contents: read
  pull-requests: write

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run unit tests
        id: unit
        continue-on-error: true
        timeout-minutes: 5
        run: npm run test:unit 2>&1 | tee unit.log

      - name: Run integration tests
        id: integration
        continue-on-error: true
        timeout-minutes: 5
        run: npm run test:integration 2>&1 | tee integration.log

      # Emit annotations (always run)
      - name: Emit test annotations
        if: always()
        run: |
          if [ "${{ steps.unit.outcome }}" != "success" ]; then
            echo "::error::Unit tests failed"
          else
            echo "::notice::Unit tests passed"
          fi
          
          if [ "${{ steps.integration.outcome }}" != "success" ]; then
            echo "::error::Integration tests failed"
          else
            echo "::notice::Integration tests passed"
          fi

      # Comment on PR (only for pull requests)
      - name: Comment test results on PR
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            let unitOutput = '';
            let integrationOutput = '';
            
            try {
              unitOutput = fs.readFileSync('unit.log', 'utf8');
            } catch (e) {
              unitOutput = 'No unit test output found';
            }
            
            try {
              integrationOutput = fs.readFileSync('integration.log', 'utf8');
            } catch (e) {
              integrationOutput = 'No integration test output found';
            }
            
            // Use step outcomes for accurate pass/fail status
            const unitPassed = '${{ steps.unit.outcome }}' === 'success';
            const integrationPassed = '${{ steps.integration.outcome }}' === 'success';


            const unitIcon = unitPassed ? '✅' : '❌';
            const integrationIcon = integrationPassed ? '✅' : '❌';
            const statusText = unitPassed && integrationPassed ? '✅ All tests passed' : '❌ Some tests failed';
            
            const body = '## 🧪 Test Results\n\n' +
              '### ' + unitIcon + ' Unit Tests\n' +
              '```\n' +
              unitOutput.slice(-30000) + '\n' +
              '```\n\n' +
              '### ' + integrationIcon + ' Integration Tests\n' +
              '```\n' +
              integrationOutput.slice(-30000) + '\n' +
              '```\n\n' +
              '---\n' +
              '**Status**: ' + statusText;

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });


      # Fail the job if tests failed
      - name: Check test results
        if: always()
        run: |
          echo "Unit outcome: ${{ steps.unit.outcome }}"
          echo "Integration outcome: ${{ steps.integration.outcome }}"
          
          # Check for timeout or failure
          if [ "${{ steps.unit.outcome }}" == "cancelled" ]; then
            echo "⏱️ Unit tests timed out (consider increasing timeout or optimizing tests)"
            exit 1
          fi
          
          if [ "${{ steps.integration.outcome }}" == "cancelled" ]; then
            echo "⏱️ Integration tests timed out (consider increasing timeout or optimizing tests)"
            exit 1
          fi
          
          if [ "${{ steps.unit.outcome }}" != "success" ] || [ "${{ steps.integration.outcome }}" != "success" ]; then
            echo "❌ Tests failed!"
            exit 1
          fi
          
          echo "✅ All tests passed!"
